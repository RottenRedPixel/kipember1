These migrations are the archived SQLite migration history from before the app moved to PostgreSQL.

They are kept for historical reference only.

The active migration history for PostgreSQL now lives in:

- `prisma/migrations`

Prisma should not attempt to replay the archived SQLite migrations against a PostgreSQL database.
